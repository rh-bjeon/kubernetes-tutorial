[.console-input]
[source,bash,subs="+macros"]
----
NODE=$(kubectl get pod -o jsonpath='{.items[0].spec.nodeName}') #<.>
echo ${NODE}

----
<.> ``.items[0]``을 사용하는 이유는 모든 Pod를 조회하고 있지만, 목록에 항목이 하나만 있다는 것을 알고 있기 때문입니다.