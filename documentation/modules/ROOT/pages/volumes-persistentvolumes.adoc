= Volume과 Persistent Volume
include::_attributes.adoc[]
:watch-terminal: 터미널 2
:file-watch-terminal: 터미널 3

컨테이너는 정의상 일시적입니다. 즉, 실행 중에 저장된 내용은 컨테이너가 멈추면 사라집니다.
이는 데이터베이스 컨테이너처럼 데이터를 영구적으로 보존해야 하는 컨테이너에 문제가 될 수 있습니다.

쿠버네티스 volume은 Pod 안의 Container들이 접근할 수 있는 디렉터리일 뿐입니다.
이 개념은 Docker volume과 비슷하지만, Docker에서는 컨테이너를 컴퓨터 호스트에 매핑하는 반면, 쿠버네티스 volume의 경우에는 그 volume을 뒷받침하는 매체와 내용이 사용된 특정 volume 유형에 따라 결정됩니다.

몇 가지 volume 유형은 다음과 같습니다:

* awsElasticBlockStore
* azureDisk
* cephfs
* nfs
* local
* empty dir
* host path

== 준비

=== 네임스페이스

:section-k8s: volumes
:set-namespace: myspace

올바른 네임스페이스 ``{set-namespace}``가 생성되어 있고 컨텍스트가 이를 가리키도록 설정되어 있는지 확인합니다.

include::partial$namespace-setup-tip.adoc[]

include::partial$set-context.adoc[]

=== 지켜보기

아직 열려 있지 않다면, 현재 네임스페이스의 pod에서 벌어지는 일을 지켜볼 터미널을 하나 열어두세요(**{watch-terminal}**이라고 부르겠습니다).

:section-k8s: volumes
include::partial$watching-pods-with-nodes.adoc[]

== Volume

``Volume``의 예시 두 가지로 시작해 봅시다.

=== EmptyDir

`emptyDir` volume은 Pod가 노드에 할당될 때 처음 생성되며, 그 Pod가 해당 노드에서 실행되는 동안 존재합니다.
이름 그대로, 처음에는 비어 있습니다.
같은 Pod 안의 모든 Container는 동일한 `emptyDir` volume을 읽고 쓸 수 있습니다.
Pod가 재시작되거나 제거되면 ``emptyDir``의 데이터는 영구적으로 사라집니다.

:quick-open-file: myboot-pod-volume.yml

파일에 내용을 쓰는 엔드포인트 하나와 그 파일에서 내용을 조회하는 엔드포인트 하나, 이렇게 두 엔드포인트를 노출하는 service를 배포해 봅시다. ``{quick-open-file}``을 열어보세요.

include::partial$tip_vscode_quick_open.adoc[]

[source, yaml]
.{quick-open-file}
----
include::example$myboot-pod-volume.yml[]
----
<.> 이것이 ``Deployment``가 아니라 ``Pod``라는 점에 주목하세요.
<.> 이 마운트 지점이 pod 안에 나타날 위치입니다. 아래를 참고하세요.
<.> 이 값은 우리가 정의할 volume의 이름과 일치해야 하며, 이 경우 파일 맨 아래에 정의되어 있습니다.

`volumes` 섹션에서는 volume을 정의하고, `volumeMounts` 섹션에서는 그 volume이 컨테이너 안에 어떻게 마운트되는지를 정의합니다.

[.console-input]
[source,bash,subs="+macros,+attributes"]
----
kubectl apply -f apps/kubefiles/myboot-pod-volume.yml
----

그러면 watch 창에서 다음과 비슷한 내용을 볼 수 있습니다.

[tabs]
====
{watch-terminal}::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME         READY  STATUS             AGE  NODE
myboot-demo  0/1    ContainerCreating  9s   devnation
----
--
====

pod가 실행 중이면, 컨테이너에 exec로 접속해 봅시다:

[.console-input]
[source,bash]
----
kubectl exec -ti myboot-demo -- /bin/bash
----

컨테이너에 ``exec``로 접속했으면, 다음 명령을 실행합니다:

[tabs]
====
컨테이너::
+
--
[.console-input]
[source,bash]
----
curl localhost:8080/appendgreetingfile
curl localhost:8080/readgreetingfile
----

그러면 다음이 반환됩니다.

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
Jambo
----

이 경우 ``emptyDir``이 ``/tmp/demo``로 설정되어 있으므로, ``ls``를 실행해서 디렉터리 내용을 확인할 수 있습니다:

[.console-input]
[source,bash]
----
ls /tmp/demo
----

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
greeting.txt
----
--
====

==== EmptyDir의 일시성

아직 닫지 않았다면 컨테이너의 셸을 닫으세요:

[tabs]
====
컨테이너::
+
--
[.console-input]
[source,bash]
----
exit
----
--
====

그리고 pod를 삭제합니다:

[.console-input]
[source,bash]
----
kubectl delete pod myboot-demo
----

[IMPORTANT]
====
다시 배포를 시도하기 전에 pod가 완전히 삭제될 때까지 기다려야 합니다.
====

그런 다음 같은 service를 다시 배포하면:

[.console-input]
[source,bash,subs="+macros,+attributes"]
----
kubectl apply -f apps/kubefiles/myboot-pod-volume.yml
----

그리고 `Running` 상태가 되면 pod에 ``exec``로 접속합니다:

[.console-input]
[source,bash]
----
kubectl exec -ti myboot-demo -- /bin/bash
----

[tabs]
====
컨테이너::
+
--

새 pod의 마운트 지점 내용을 조회해 봅시다.

[.console-input]
[source,bash]
----
ls /tmp/demo
----

디렉터리 내용이 비어 있는 것을 확인할 수 있는데, 이는 이전 pod에서 만든 파일이 pod가 삭제되면서 함께 사라졌다는 의미입니다.

[.console-output]
[source,bash]
----
root@myboot-demo:/app# 
----

pod를 나갑니다.

[.console-input]
[source,bash]
----
exit
----

--
====

이제 pod를 삭제합니다.

[.console-input]
[source,bash]
----
kubectl delete pod myboot-demo
----

==== Pod 안에서 EmptyDir 공유하기

``emptyDir``은 같은 Pod의 컨테이너 사이에 공유됩니다. 같은 pod 안에 같은 `emptyDir` volume을 마운트하는 두 개의 컨테이너를 만드는 deployment를 살펴봅시다.

:quick-open-file: myboot-pods-volume.yml
include::partial$tip_vscode_quick_open.adoc[]

``{quick-open-file}``을 살펴봅시다:

[.console-output]
[source,yaml]
.{quick-open-file}
----
include::example$myboot-pods-volume.yml[]
----
<.> pod의 첫 번째 컨테이너는 myboot-demo-1이라는 이름을 가지며, ``/tmp/demo``에 ``demo-volume``을 마운트합니다.
<.> pod의 두 번째 컨테이너는 ``myboot-demo-2``라는 이름을 가지며, 같은 `/tmp/demo` 지점에 ``demo-volume``을 마운트합니다.
<.> 두 컨테이너는 완전히 동일한 이미지를 사용합니다.
<.> 컨테이너들이 pod의 포트를 공유하기 때문에 두 번째 컨테이너는 첫 번째와 다른 포트를 리슨해야 한다는 점에 주목하세요. 이 수준의 `env` 지시자는 `myboot-demo-2` 컨테이너에만 적용됩니다.
<.> volume은 한 번만 정의되지만 pod의 각 컨테이너에서 참조됩니다.

이제 `{set-namespace}` 네임스페이스에 이 deployment를 만들어 봅시다.

[.console-input]
[source,bash]
----
kubectl apply -f apps/kubefiles/myboot-pods-volume.yml
----

그러면 pod watch에서 다음을 볼 수 있습니다.

[tabs]
====
{watch-terminal}::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes,+quotes"]
----
NAME          READY   STATUS    RESTARTS   AGE
myboot-demo   #2/2#    Running   0          4s
----

`2/2` ready 상태에 주목하세요. 이는 pod 정의의 컨테이너 2개를 나타냅니다.
--
====

먼저, pod의 **두 번째** 컨테이너에 exec로 접속해서 마운트 지점에 대한 watch를 시작해 봅시다. 이를 위해 또 다른 터미널(**{file-watch-terminal}**)을 열어서, pod의 다른 컨테이너에 ``exec``로 접속해 `cat` 명령을 실행합니다.

[tabs]
====
{file-watch-terminal}::
+
--

include::partial$open-terminal-in-editor-inset.adoc[]

[.console-input]
[source,bash]
----
kubectl exec -it myboot-demo -c myboot-demo-2 -- bash 
----

그런 다음 pod의 `myboot-demo-2` 컨테이너 안에서 다음 명령을 실행합니다:

[.console-input]
[source,bash]
----
watch -n1 -- "ls -l /tmp/demo && eval ""cat /tmp/demo/greeting.txt"""
----

그러면 처음에는 다음이 반환됩니다.

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
total 0
cat: /tmp/demo/greeting.txt: No such file or directory
----

--
====

메인 터미널에서 **첫 번째** 컨테이너에 접속해서, **두 번째** 컨테이너가 볼 수 있는 파일을 만들 수 있는지 확인해 봅시다.

[tabs]
====
터미널 1::
+
--
[.console-input]
[source,bash]
----
kubectl exec -ti myboot-demo -c myboot-demo-1 -- /bin/bash
----

그리고 `/tmp/demo` 디렉터리에 콘텐츠를 생성합니다.

[.console-input]
[source,bash]
----
curl localhost:8080/appendgreetingfile
----

그런 다음 파일이 존재하는지, 그 내용은 무엇인지 확인합니다:

[.console-input]
[source,bash]
----
ls -l /tmp/demo && echo $(cat /tmp/demo/greeting.txt) 
----

[.console-output]
[source,bash]
----
total 4
-rw-r--r--. 1 root root 5 Jul 13 08:11 greeting.txt
Jambo
----

--
====

한편 **{file-watch-terminal}**에서는 다음과 비슷한 내용을 볼 수 있을 것입니다:

[tabs]
====
{file-watch-terminal}::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
total 4
-rw-r--r--. 1 root root 5 Jul 13 08:11 greeting.txt
Jambo
----

kbd:[CTRL+c]를 눌러 watch를 종료한 다음, pod에 대한 ``exec``도 종료합니다.

[.console-input]
[source,bash]
----
exit
----

이제 터미널로 돌아와서, 다음 명령으로 Pod의 volume 정보를 확인할 수 있습니다:

[.console-input]
[source,bash]
----
kubectl describe pod myboot-demo
----

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
Volumes:
  demo-volume:
    Type:       EmptyDir (a temporary directory that shares a pods lifetime)
    Medium:
    SizeLimit:  <unset>
----

--
====

==== 정리

include::partial$terminal-cleanup.adoc[tags=**;!*;term3;term-exec]

=== HostPath

:quick-open-file: myboot-pod-volume-hostpath.yml

`hostPath` volume은 노드의 파일시스템에 있는 파일이나 디렉터리를 Pod 안에 마운트합니다. ``{quick-open-file}``을 살펴보세요.

include::partial$tip_vscode_quick_open.adoc[]

[source, yaml]
.{quick-open-file}
----
include::example$myboot-pod-volume-hostpath.yaml[]
----
<.> 이전과 동일한 위치를 마운트하지만, 여기서는 volume을 `emptyDir` 대신 ``hostPath``로 정의한 것을 볼 수 있습니다.
<.> ``/mnt/data``는 이 pod가 할당되는 쿠버네티스 ``node``의 한 위치입니다.

이 경우, 콘텐츠가 저장될 host/node 디렉터리를 정의하는 것입니다.

[.console-input]
[source,bash]
----
kubectl apply -f apps/kubefiles/myboot-pod-volume-hostpath.yaml
----

이제 Pod를 describe해 보면, volumes 섹션에서 다음을 볼 수 있습니다:

[.console-input]
[source,bash]
----
kubectl describe pod myboot-demo
----

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
Volumes:
  demo-volume:
    Type:          HostPath (bare host directory volume)
    Path:          /mnt/data
    HostPathType:
----


[tabs]
====
{file-watch-terminal}::
+
--
'host' 또는 'node'의 디렉터리를 지켜볼 수 있는 터미널을 열어봅시다.

include::partial$open-terminal-in-editor-inset.adoc[]


:mount-dir: /mnt/data
include::partial$watch-node-directory.adoc[]

--
====

[tabs]
====
Terminal 1::
+
--

include::partial$create-greeting-file.adoc[]

--
====

한편 다른 터미널(**{file-watch-terminal}**)에서는 동시에 watch 출력이 바뀌는 것을 볼 수 있습니다.

[tabs]
====
{file-watch-terminal}::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
Every 1.0s: eval ls -al /mnt/da...  devnation: Tue Jul 13 09:14:28 2021

total 4
drwxr-xr-x. 1 root root 24 Jul 13 09:13 .
drwxr-xr-x. 1 root root  8 Jul 13 08:24 ..
-rw-r--r--. 1 root root  5 Jul 13 09:13 greeting.txt
Jambo
----
--
====

이제 Pod 안의 ``/tmp/demo``에 저장된 콘텐츠가 host 경로 ``/mnt/data``에 저장되므로, Pod가 죽더라도 콘텐츠는 사라지지 않는다는 점에 주목하세요.

하지만 Pod가 다운되어 다른 노드에 다시 스케줄되는 경우에는, 그 다른 노드에 데이터가 없을 것이기 때문에 이것이 모든 문제를 해결해주지는 않을 수 있습니다.

다른 예시로, 이번에는 Amazon EBS Volume을 살펴봅시다:

[source, yaml]
----
apiVersion: v1
kind: Pod
metadata:
  name: test-ebs
spec:
...  
  volumes:
    - name: test-volume
      awsElasticBlockStore:
        volumeID: <volume-id>
        fsType: ext4
----

앞의 스니펫에서 주목해야 할 점은, 애플리케이션에 속한 것들(즉 컨테이너, probe, 포트 등)로 더 __dev__ 쪽에 가까운 것과, 클라우드와 더 관련된 것들(즉 물리적 스토리지)로 더 __ops__ 쪽에 가까운 것이 뒤섞여 있다는 것입니다.

이런 개념의 뒤섞임을 피하기 위해, 쿠버네티스는 어느 정도 추상화 계층을 제공해서, 개발자는 그저 데이터를 저장할 공간을 요청하고(__persistent volume claim__), 운영팀이 물리적인 스토리지 설정을 제공하게 합니다.

==== 정리

include::partial$terminal-cleanup.adoc[tags=**;!*;term-exec]

== Persistent Volume과 Persistent Volume Claim

`PersistentVolume`(__PV__)은 관리자가 만들거나, ``Storage Classes``를 사용해 Pod와 독립적으로 동적으로 생성되는 쿠버네티스 리소스입니다.
이는 스토리지 구현의 세부 사항을 담고 있으며, NFS, Ceph, iSCSI, 또는 클라우드 제공업체 전용 스토리지 시스템일 수 있습니다.

`PersistentVolumeClaim`(__PVC__)은 사용자의 스토리지 요청입니다.
특정 volume 크기나, 예를 들어 접근 모드를 요청할 수 있습니다.

=== hostPath를 사용한 persistent volume/claim

:quick-open-file: demo-persistent-volume-hostpath.yaml

`hostPath` 전략을 사용하되, volume으로 직접 설정하지 않고 persistent volume과 persistent volume claim을 사용해 봅시다. ``{quick-open-file}``을 확인해 보세요:

include::partial$tip_vscode_quick_open.adoc[]

[source, yaml]
.{quick-open-file}
----
kind: PersistentVolume
apiVersion: v1
metadata:
  name: my-persistent-volume
  labels:
    type: local
spec:
  storageClassName: pv-demo 
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/persistent-volume"
----

이제 `volume` 정보는 더 이상 pod에 있지 않고 __persistent volume__ 객체에 있습니다.

[.console-input]
[source,bash]
----
kubectl apply -f apps/kubefiles/demo-persistent-volume-hostpath.yaml 

kubectl get pv -w
----

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                                       CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS      CLAIM                                           STORAGECLASS   REASON   AGE
my-persistent-volume                       100Mi      RWO            Retain           Available                                                   pv-demo                 5s
----

:mount-dir: /mnt/persistent-volume

volume이 만들어졌으면, 파일 watch 터미널이 volume의 새 위치인 ``{mount-dir}``을 보도록 업데이트해 봅시다.

[tabs]
====
{file-watch-terminal}::
+
--

kbd:[CTRL+c]를 눌러 현재 watch를 종료합니다.

그런 다음 새 watch를 시작합니다.

include::partial$file-watch-command.adoc[]
--
====

:quick-open-file: myboot-persistent-volume-claim.yaml

그런 다음 dev 쪽에서는, __PV__로부터 필요한 것을 claim해야 합니다.
다음 예시에서는 *10Mi* 공간을 요청합니다. ``{quick-open-file}``을 확인하세요:

include::partial$tip_vscode_quick_open.adoc[]

[source, yaml]
.{quick-open-file}
----
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: myboot-volumeclaim
spec:
  storageClassName: pv-demo 
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
----


[.console-input]
[source,bash]
----
kubectl apply -f apps/kubefiles/myboot-persistent-volume-claim.yaml

kubectl get pvc -w
----

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                 STATUS   VOLUME                 CAPACITY   ACCESS MODES   STORAGECLASS   AGE
myboot-volumeclaim   Bound    my-persistent-volume   100Mi      RWO            pv-demo        3s
----

:quick-open-file: myboot-pod-volume-pvc.yaml

큰 차이점은, 이제 pod의 `volumes` 섹션에는 volume 설정을 직접 정의하는 것이 아니라, 사용할 __persistent volume claim__만 정의한다는 것입니다. ``{quick-open-file}``을 확인하세요:

include::partial$tip_vscode_quick_open.adoc[]

[source, yaml]
.{quick-open-file}
----
apiVersion: v1
kind: Pod
metadata:
  name: myboot-demo
spec:
  containers:
  - name: myboot-demo
    image: quay.io/rhdevelopers/myboot:v4
    
    volumeMounts:
    - mountPath: /tmp/demo
      name: demo-volume

  volumes:
  - name: demo-volume
    persistentVolumeClaim:
      claimName: myboot-volumeclaim
----

[.console-input]
[source,bash]
----
kubectl apply -f apps/kubefiles/myboot-pod-volume-pvc.yaml

kubectl describe pod myboot-demo
----

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
Volumes:
  demo-volume:
    Type:       PersistentVolumeClaim (a reference to a PersistentVolumeClaim in the same namespace)
    ClaimName:  myboot-volumeclaim
    ReadOnly:   false
----

이제 pod의 describe 내용을 보면, volume이 직접 설정된 것이 아니라 persistent volume claim을 통해 설정된 것을 확인할 수 있습니다.

include::partial$create-greeting-file.adoc[]

이 작업을 마치자마자, ``PersistentVolume``이 매핑되는 노드의 경로에서 다음과 같은 내용을 볼 수 있을 것입니다:

[tabs]
====
{file-watch-terminal}::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
Every 1.0s: ls -al {mount-dir} && eval c...  devnation: Mon Jul 19 14:07:53 2021

total 4
drwxr-xr-x. 1 root root 24 Jul 19 14:06 .
drwxr-xr-x. 1 root root 42 Jul 13 09:21 ..
-rw-r--r--. 1 root root  5 Jul 19 14:06 greeting.txt
Jambo
----
--
====

==== 정리

include::partial$terminal-cleanup.adoc[tags=**;!*;term-exec;term3-ssh]

정리가 끝나면 다음을 실행합니다:

[.console-input]
[source,bash,subs="+macros,+attributes"]
----
kubectl get pvc
----

결과:

[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                 STATUS   VOLUME                 CAPACITY   ACCESS MODES   STORAGECLASS   AGE
myboot-volumeclaim   Bound    my-persistent-volume   100Mi      RWO            pv-demo        14m
----

pod가 삭제되었더라도 PVC(그리고 PV)는 여전히 남아 있으며, 수동으로 삭제해야 합니다.

[.console-input]
[source,bash]
----
kubectl delete -f apps/kubefiles/myboot-persistent-volume-claim.yaml
kubectl delete -f apps/kubefiles/demo-persistent-volume-hostpath.yaml
----

== 정적 vs 동적 프로비저닝

Persistent Volume은 동적으로 또는 정적으로 프로비저닝될 수 있습니다.

정적 프로비저닝은 클러스터 관리자가 **기존** 스토리지 장치를 클러스터에서 사용할 수 있게 해줍니다.
이 방식에서는 PV와 PVC를 수동으로 제공해야 합니다.

지금까지 마지막 예시에서 본 것이 바로 정적 프로비저닝입니다.

동적 프로비저닝은 클러스터 관리자가 미리 스토리지를 프로비저닝해야 할 필요성을 없애줍니다.
대신, 사용자가 요청하면 스토리지를 자동으로 프로비저닝합니다.
이를 동작시키려면 Storage Class 객체와 그것을 참조하는 PVC를 제공해야 합니다.
PVC가 생성되고 나면, 스토리지 장치와 PV가 자동으로 만들어집니다.
동적 프로비저닝의 주된 목적은 클라우드 제공업체 솔루션과 함께 동작하는 것입니다.

일반적으로 쿠버네티스 구현체는 기본 Storage Class를 제공해서 누구나 동적 프로비저닝을 빠르게 시작할 수 있게 해줍니다.
다음 명령으로 기본 Storage Class의 정보를 확인할 수 있습니다:

[.console-input]
[source,bash]
----
kubectl get sc
----

[tabs]
====
Minikube::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                 PROVISIONER                RECLAIMPOLICY   VOLUMEBINDINGMODE   ALLOWVOLUMEEXPANSION   AGE
standard (default)   k8s.io/minikube-hostpath   Delete          Immediate           false                  47d
----
--
OpenShift::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME            PROVISIONER             AGE
gp2 (default)   kubernetes.io/aws-ebs   31h
----

OpenShift가 클라우드 제공업체에 설치되면 기본적으로 그 클라우드의 기반 영구 스토리지 기술을 사용하는 Storage Class를 자동으로 생성합니다.
예를 들어 AWS의 경우, AWS EBS를 가리키는 기본 Storage Class가 제공됩니다.
--
====

그런 다음 Persistent Volume Claim을 생성하면 Persistent Volume이 자동으로 생성됩니다. kbd:[CTRL+p]를 사용해서 ``demo-dynamic-persistent.yaml``을 빠르게 열어보세요:

[source, yaml]
----
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: myboot-volumeclaim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
----

__storage class__를 따로 지정하지 않았지만 기본값으로 정의된 것이 있으므로, __PVC__는 암묵적으로 그것을 참조합니다. (이 pod 정의를 ``myboot-persistent-volume-claim.yaml``과 비교해 보는 것도 좋습니다.)

.정적 PVC와 동적 PVC(정적 PV)의 차이
image::pv-static-vs-dynamic.png[]

[.console-input]
[source,bash]
----
kubectl apply -f apps/kubefiles/demo-dynamic-persistent.yaml

kubectl get pvc
----

[tabs]
====
Minikube::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                 STATUS    VOLUME   CAPACITY   ACCESS MODES   STORAGECLASS   AGE
myboot-volumeclaim   Pending                                      standard       2s
----
--
OpenShift::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                 STATUS    VOLUME   CAPACITY   ACCESS MODES   STORAGECLASS   AGE
myboot-volumeclaim   Pending                                      gp2            46sç
----
--
====

__PVC__가 __Pending__ STATUS인 것에 주목하세요. 우리가 만들고 있는 것이 동적 스토리지라는 것을 기억해 보면, 이는 __pod__가 volume을 요청하기 전까지는 __PVC__가 pending 상태로 남아 있고 __PV__도 생성되지 않는다는 의미입니다.

[.console-input]
[source,bash]
----
kubectl apply -f apps/kubefiles/myboot-pod-volume-pvc.yaml
----

[tabs]
====
{watch-terminal}::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME          READY   STATUS    RESTARTS   AGE
myboot-demo   1/1     Running   0          2m36s
----
--
====

pod가 __Running__ 상태가 되면, __PVC__와 __PV__의 매개변수를 확인할 수 있습니다.

[.console-input]
[source,bash]
----
kubectl get pvc
----

[tabs]
====
Minikube::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                 STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
myboot-volumeclaim   Bound    pvc-170f2e9a-4afc-4869-bd19-f10c86bff34b   10Mi       RWO            standard       5s
----
--
OpenShift::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                 STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
myboot-volumeclaim   Bound    pvc-6de4f27e-bd40-4b58-bb46-91eb08ca5bd7   1Gi        RWO            gp2            116s
----
--
====


이제 volume claim이 volume에 __Bound__ 된 것에 주목하세요.

마지막으로, __PV__가 자동으로 생성되었는지 확인할 수 있습니다:

[.console-input]
[source,bash]
----
kubectl get pv
----

[tabs]
====
Minikube::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                                       CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS   CLAIM                        STORAGECLASS   REASON   AGE
pvc-170f2e9a-4afc-4869-bd19-f10c86bff34b   10Mi       RWO            Delete           Bound    myspace/myboot-volumeclaim   standard                56s
----
--
OpenShift::
+
--
[.console-output]
[source,bash,subs="+macros,+attributes"]
----
NAME                                       CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS   CLAIM                        STORAGECLASS   REASON   AGE
pvc-6de4f27e-bd40-4b58-bb46-91eb08ca5bd7   1Gi        RWO            Delete           Bound    default/myboot-volumeclaim   gp2                     77s
----
--
====

__CLAIM__ 필드가 __PV__ 생성을 담당한 __PVC__를 가리키는 것에 주목하세요.

=== 정리

[.console-input]
[source,bash]
----
kubectl delete -f apps/kubefiles/myboot-pod-volume-pvc.yaml
kubectl delete -f apps/kubefiles/demo-dynamic-persistent.yaml
----

== 분산 파일 시스템

클라우드 제공업체는 데이터가 모든 노드에서 항상 사용 가능하도록 분산 스토리지를 제공한다는 점을 알아두는 것이 중요합니다.
마지막 예시에서 봤듯이, 이 storage class는 모든 노드가 동일한 디스크 콘텐츠를 보도록 보장합니다.

예를 들어, 온프레미스에서 쿠버네티스/OpenShift를 사용하고 있거나 특정 벤더 솔루션에 의존하고 싶지 않다면, 쿠버네티스에서도 분산 파일 시스템을 지원합니다.
그런 경우라면 NFS, https://www.gluster.org/[GlusterFS] 또는 https://ceph.io/[Ceph]를 사용하는 것을 권장합니다.
